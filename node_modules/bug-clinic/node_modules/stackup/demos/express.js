require('../index.js');

var fs      = require('fs');
var express = require('express');
var levelup = require('levelup')

var app     = express();

app.get('/', function (req, res) {
  require('http').request('http://google.com/', function AskGoogle(out) {
    out.pipe(res);
    throw new Error();
  }).end();
})

app.listen(8080, function () {
  require('http').request('http://localhost:8080/').end();
});
